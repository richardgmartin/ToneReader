# Tone Reader
  
  use_frameworks!

target 'ToneReader' do
  
  platform :ios, '9.0'

  pod 'Firebase/Storage'
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Firebase/RemoteConfig'
  pod 'FirebaseUI'
  pod 'FirebaseUI/Auth'
  pod 'FirebaseUI/Google'

  target 'ToneReaderTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'ToneReader WatchKit App' do

    platform :watchos, '2.0'

  # Pods for ToneReader WatchKit App

end

target 'ToneReader WatchKit Extension' do
     
     platform :watchos, '2.0'

  # Pods for ToneReader WatchKit Extension

end
